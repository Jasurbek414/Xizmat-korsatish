; Asterisk'ning o'rnatilgan HTTP serveri - ARI (REST+WebSocket) va PJSIP'ning
; WebSocket transporti (transport-ws, pjsip.conf) shu server ustida ishlaydi.
; MUHIM: faqat ICHKI Docker tarmog'i uchun (docker-compose.yml'da host'ga
; port ochilmagan - xuddi avvalgi FreeSWITCH ESL porti 8021 kabi) - Java
; backend va nginx konteyner nomi ("asterisk") orqali ichkaridan yetadi.
[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
